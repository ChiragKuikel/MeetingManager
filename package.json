{
  "name": "ai-meeting-summarizer",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d && concurrently -n api,worker,frontend -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev:worker --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
